import AppleIcon from "../assets/Apple_gray_logo 1.png";
const AppleLogo = ({ className }) => {
  return (
    <img
      src={AppleIcon}
      className={className}
      width={40}
      height={49}
      alt="apple logo"
      srcset=""
    />
  );
};

export default AppleLogo;
